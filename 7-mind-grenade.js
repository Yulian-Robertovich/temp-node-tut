const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`The sum is : ${num1 + num2}`);
}
addValues();
//If we have a function within our module (no need for exports calling) we simly use require in our app file
